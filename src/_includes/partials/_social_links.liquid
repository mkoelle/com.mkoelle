{%- for social in socials %}
<a href="{{ social.url }}" target="_blank" rel="noopener noreferrer" aria-label="{{ social.title }}">
  <div class="bx {{ social.logo }} text-2xl hover:text-orange-400"></div>
</a>
{%- endfor %}