<nav>
  {%- for p in collections.page %}
    <a href="{{ p.url }}" {% if page.url == p.url %}
        aria-current="page"
        data-state="active"
    {% endif %}>
      {{ p.data.title }}
    </a>
  {%- endfor %}
</nav>
<div class="h-3 w-full bg-gradient-to-b from-black" />